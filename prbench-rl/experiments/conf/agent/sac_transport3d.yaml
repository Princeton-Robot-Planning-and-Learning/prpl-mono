# SAC agent configuration for Transport3D
name: sac

# Core training parameters
args:
  total_timesteps: 1000000
  num_eval_envs: 1
  hidden_size: 128

# Training settings
torch_deterministic: true
cuda: false

# Logging settings
exp_name: "sac_transport3d_train"
tb_log_dir: "runs"
