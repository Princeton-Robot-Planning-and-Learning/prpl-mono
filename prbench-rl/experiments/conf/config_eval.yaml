# Default configuration for RL experiments
defaults:
  - agent: ppo_motion2d_0_passage
  - _self_

# Experiment mode: "train" or "eval"
mode: eval
env_id: prbench/Motion2D-p0-v0
max_episode_steps: 300
load_agent_path: runs/ppo_m2d_0_passage_train_1119_1m/policies/ckpt_972800.pt
eval_episodes: 50

# Random seed
seed: 0